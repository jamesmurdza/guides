# Get your API key from https://app.daytona.io/dashboard/keys
DAYTONA_API_KEY=

# OpenAI API key, used as Goose's model provider.
# Injected into the sandbox so the Goose CLI runs fully headless.
SANDBOX_OPENAI_API_KEY=
