[project]
name = "windows-computer-use-evals"
version = "0.1.0"
description = "Run Windows computer-use evals on Daytona sandboxes"
requires-python = ">=3.10"
dependencies = [
    "daytona>=0.193.0",
]

[tool.setuptools]
py-modules = ["common", "tasks", "harness", "prep_snapshot"]
